title = "uintdev blog"
description = "a few technical insights"
author = "uintdev"

# The URL the site will be built for
base_url = "/"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# When set to "true", the HTML output files will be minified
minify_html = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

# Generates info for RSS and ATOM readers
generate_feeds = true
feed_filenames = ["rss.xml"]

[markdown.highlighting]
theme = "github-dark-default"

[extra]
# Put all your custom variables here
base_domain = "https://blog.uint.dev"
author = "uintdev"
github_account = "uintdev"
main_website = "https://uint.dev/"
